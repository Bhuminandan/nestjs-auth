import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Nest auth backend',
  description: 'These apis are used for Nest auth backend.',
  version: '0.0.1',
};
